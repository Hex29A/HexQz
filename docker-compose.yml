services:
  quiz:
    build: .
    ports:
      - "${PORT:-3042}:3042"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    env_file: .env
    environment:
      - NODE_ENV=production
      - PORT=3042
      - DB_PATH=/app/data/hexqz.sqlite
    restart: unless-stopped
